[project]
name = "python-web-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "gunicorn>=26.0.0",
    "httpx>=0.28.1",
    "requests>=2.33.1",
]
