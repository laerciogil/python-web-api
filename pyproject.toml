[project]
name = "python-web-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["flask-blog", "django-blog"]

[tool.uv]
managed = true
package = false

[tool.uv.workspace]
members = ["exemplos/day2/django", "exemplos/day2/flask"]

[tool.uv.sources]
flask-blog = {workspace = true}
django-blog = {workspace = true}

[dependency-groups]
dev = [
    "flask-shell-ipython>=0.5.3",
]
